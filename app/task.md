# App Implementation Checklist

- [ ] Initialize Node.js project (`npm init -y`) strictly inside `/app`.
- [ ] Install Express.js (`npm install express`) inside `/app`.
- [ ] Create `server.js` matching the constraints in `requirements.md`.
- [ ] Update `package.json` with a `"start": "node server.js"` script.
